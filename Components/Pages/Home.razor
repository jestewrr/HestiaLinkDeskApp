@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // This automatically redirects the root URL ("/") to the login page
        Navigation.NavigateTo("/login");
    }
}